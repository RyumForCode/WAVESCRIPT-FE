const NovelContent = () => {

    const title = 'title';
    const genre = 'genre';
    const creator = 'creator';
    const contributor = 'contributor';
    const status = 'status';

    return (
        <div>
            <span>{title}</span>
            <span>{genre}</span>
            <span>{creator}</span>
            <span>{contributor}</span>
            <span>{status}</span>
        </div>
    );
};

export default NovelContent;